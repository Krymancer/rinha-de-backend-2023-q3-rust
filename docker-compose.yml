version: '3.5'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    hostname: api
    depends_on:
      - db
    ports:
      - "9999:1234"
    deploy:
      resources:
        limits:
          cpus: '0.75'
          memory: '1.5GB'
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: user
      POSTGRES_DB: db
    deploy:
      resources:
        limits:
          cpus: '0.75'
          memory: '1.5GB'